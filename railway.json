{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "sh -c 'python -c \"from backend.database import engine, Base; from backend.models import *; Base.metadata.create_all(bind=engine)\" && uvicorn backend.main:app --host 0.0.0.0 --port ${PORT:-8000}'",
    "healthcheckPath": "/",
    "restartPolicyType": "ON_FAILURE"
  }
}
